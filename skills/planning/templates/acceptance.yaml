feature: <feature-name>
evaluator_ref: PT-EVAL

scenarios:
  - id: AC-1
    type: death_path
    source_refs: [PT-D1]
    given: "<evidence-backed precondition>"
    when: "<action>"
    then:
      - "<observable outcome>"

  - id: AC-2
    type: happy_path
    source_refs: [PT-D1]
    given: "<precondition>"
    when: "<action>"
    then:
      - "<observable outcome>"
      - "<evidence required to call this success>"

not_applicable: []
# When a type truly cannot occur:
#   - type: <degradation | unknown_outcome>
#     rationale: "<why this outcome cannot occur for this feature>"
