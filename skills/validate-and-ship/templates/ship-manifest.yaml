delivered_capability: "<what was delivered>"

known_failure_modes:
  - mode: "<failure description>"
    severity: degradation  # crash | degradation | silent_corruption
    detection: "<how it's detected>"

accepted_risks:
  - risk: "<risk description>"
    accepted_by: "human"
    re_review_signal: "<observable condition that should trigger re-review — required, not a calendar date>"
    owner: "<who is responsible for noticing the signal — required, cannot be empty>"

silent_failure_surface: low  # low | medium | high

monitoring_hooks:
  - "<monitoring description>"

kill_switch: "<how to disable this feature>"
