# Auto Mode Decision Log — scar-report-subtraction

Append-only. See `references/auto-mode.md` Decision Log Contract. Do not edit
prior entries; corrections are new entries that name the superseded one.

## Decision 001 - research.research-to-pre-thinking-gate
- decision_id: decision-001
- timestamp: 2026-07-07T07:29:28Z
- stage: research
- prompt_type: confirmation
- workflow_prompt: "Research 完成。1-kickoff.md 和 problem-autopsy.md 已寫入 `changes/2026-07-07_scar-report-subtraction/`。確認後進入 Pre-thinking？"
- gatekeeper_answer: "Proceed. Both artifacts clear the research skill's own bar: Problem Essence stays in requirement language with no mechanism named ('讓讀者一分鐘內知道哪裡有疤/何時咬人/去哪查證，可讀性由機制保證非自律'); Boundary Scope gives three lists and every item in '哪些現在不做' (5 items) carries an explicit not-now reason; all 4 Must-Haves carry death conditions with concrete degrade/rollback triggers; North Star has invalidation_condition, corruption_signature, and a sub-metric with proxy_confidence: medium plus a named decoupling_detection check; the autopsy's translation_delta (2 entries), kill_conditions (4, feature-specific not generic), damage_recipients (4), and observable_done_state (3 sentences, within the ≤3 limit) are all filled with no TBD/N/A. Evidence in the kickoff was independently spot-checked and holds: task-4-scar.yaml is 232 lines (kickoff claims 233, off-by-one, immaterial); the systemic scar id `doc-vs-runtime-obedience` is genuinely registered in .samsara/systemic-scars.yaml with first_recorded 2026-07-02; validate_format.py was read and confirmed to contain zero length/budget checks (only dual-face/dangling-ref/debt-consistency shape checks), matching the kickoff's evidence claim; all 9 files named in 'Boundary Scope: 涉及哪些' exist on disk. On the flagged tension (Must-Have #1 names candidate slot fields what/bites_when/where/accepted_because, and Must-Have #2/North-Star name candidate numeric thresholds like ≤60 lines): this does not cross the essence-purity test, because that test (references/research templates/kickoff.md's 'if the essence names a mechanism, rewrite' check) is scoped explicitly to the Problem Essence line, which this kickoff keeps clean. Must-Have items in this skill's established convention are expected to be this concrete — the immediately preceding kickoff in this repo (2026-07-02_workflow-subtraction-optimization, W1-W6) names equally specific mechanisms ('systemic-scar registry', 'verified: true 壓縮為單行') at the Must-Have layer without being treated as a boundary violation. The field names are hedged with '如' (illustrative, not mandated) and the thresholds are explicitly marked 'confirmed by planning' with a death condition that hands the decision back if the structure doesn't hold up in practice — this preserves pre-thinking's freedom to derive its own structural identity rather than being handed a fait accompli."
- decision: proceed
- rationale: "All six research-skill bar items (essence purity, three-list boundary scope with reasons, must-haves with death conditions, north-star triad, and the autopsy's five required non-empty sections plus 3-sentence observable_done_state) are met with verifiable, not assumed, evidence. The one tension flagged in my brief (candidate field names / thresholds in Must-Have) is a legitimate layering question but is not a bar violation: it is scoped, hedged as illustrative, has an explicit death condition, and matches this repo's own established Must-Have convention from the prior feature. Sending this back for revise would be ceremony without a concrete defect to name — the artifacts already do the honest thing (mark thresholds as planning-calibrated, mark fields as example)."
- principles_used:
  - "research skill's own essence-purity test (templates/kickoff.md comment) applies to the Problem Essence line specifically, not to every downstream Scope item"
  - "project convention observed in 2026-07-02_workflow-subtraction-optimization kickoff: Must-Have items name concrete mechanisms; this is the established norm for this repo's Scope section, not a deviation"
  - "user-prefers-honest-process-over-ceremony (memory): match process weight to actual risk; do not revise an artifact that already hedges its own candidate status correctly"
  - "'減法的終點不是功能少，而是剩下的每個東西都有人為它的腐爛負責' — every must-have here has an owner-facing death condition, satisfying this project axiom"
- architecture_considerations:
  - "Research vs pre-thinking division of labor: research owns 'problem essence' (requirement language), pre-thinking owns 'structural identity' (what code must be). Must-Have #1 pre-names a candidate structure (fixed-length slots); flagged here so pre-thinking treats it as a hypothesis to validate/redesign, not a locked decision — this is exactly what the Death Condition and '如' hedge are for."
  - "Boundary Scope's 'not-now' list correctly keeps this feature's seam narrow (no migration of old reports, no touching review-record/transition-record containers, no semantic-quality automation, no touching systemic-scars.yaml format or iteration's signal_lost formula) — this bounds pre-thinking's structural thinking to the schema/validator/consumption-contract seam only, preventing scope creep that the autopsy's own kill_condition #4 explicitly guards against."
- evidence_checked:
  - "changes/2026-07-07_scar-report-subtraction/1-kickoff.md and problem-autopsy.md (full read)"
  - "skills/research/SKILL.md, skills/research/problem-autopsy.md, skills/research/templates/kickoff.md, skills/research/templates/problem-autopsy.md (bar definitions)"
  - "wc -l changes/2026-07-02_workflow-subtraction-optimization/scar-reports/task-4-scar.yaml -> 232 lines, matches kickoff's cited 233 within rounding"
  - "grep for doc-vs-runtime-obedience -> confirmed registered in .samsara/systemic-scars.yaml, first_recorded 2026-07-02"
  - "read skills/implement/scripts/validate_format.py -> confirmed no length/budget check exists, only shape checks (dual-face, dangling-ref, debt-consistency)"
  - "existence check on all 9 files listed in Boundary Scope '涉及哪些' -> all present"
  - "prior kickoff changes/2026-07-02_workflow-subtraction-optimization/1-kickoff.md read for Must-Have convention precedent"
- uncertainty:
  level: low
  notes: "Residual judgment call, not evidence gap: whether naming candidate slot field names in a Must-Have is 'too structural' for research is genuinely a matter of house style rather than a bright line in the skill docs. I resolved it toward proceed because the artifact itself already hedges (illustrative fields, planning-calibrated thresholds, death conditions) and matches established repo convention. If pre-thinking treats 'what/bites_when/where/accepted_because' as locked rather than as a hypothesis to test, that would be a downstream failure to watch for, not a defect in this artifact."
- consequences:
  - "Invoke samsara:pre-thinking for scar-report-subtraction. Pre-thinking should independently derive structural identity for the fixed-slot mechanism rather than treating the kickoff's candidate field names as final, per the Death Condition attached to Must-Have #1."

## Decision 002 - pre-thinking.step5.evaluation-contract-primary
- decision_id: decision-002
- timestamp: 2026-07-07T10:02:57Z
- stage: pre-thinking
- prompt_type: question
- workflow_prompt: "這個 feature 的 Primary evaluator（唯一 canonical 回饋源）是哪一個？ Option A (expanded pytest suite only, incl. length-budget behavior tests + rebound compat death tests) / Option B (golden re-expression of task-4-scar.yaml as the sole acceptance artifact, validator-green + honest-lossless-mapping table) / Option C (A as Primary, B demoted to a supporting acceptance artifact inside planning's acceptance criteria)."
- gatekeeper_answer: "Option C. Primary evaluator = the expanded pytest suite (`uv run pytest tests/` green, including new length-budget behavior tests with over-budget and clean fixtures, and the rebound compat death tests moved to their new canonical home per D3). Golden re-expression of task-4-scar.yaml is retained as a supporting acceptance artifact inside planning's acceptance criteria, not as the canonical feedback source. Trace: (1) chosen = C; (2) why not A alone: A alone gives a mechanical, reusable signal but has no way to test kill_condition #3 ('长度预算无法在不伤诚实的阈值下运作') because synthetic pytest fixtures are contrived, not real historical scar content — A alone would let the Primary evaluator go fully green while the exact failure mode this feature exists to prevent (honesty lost to compression) goes unchecked; D2 in pre-thinking.md already names this gap explicitly ('planning必须在acceptance裡放壓縮不失誠實的對照驗證'), so A-only contradicts the feature's own converged design decision. Why not B alone: B has an admitted judgment component ('誠實無損清單...部分靠 reviewer'), is a single golden example (A7's own text: '單例證據'), and per flow.md §6(2) the Primary evaluator must be reused unmodified by planning/iteration/debugging/validate-and-ship across however many implementation tasks this feature decomposes into (D1-D6 span schema restructure, validate_format.py additions, dispatch-template trims, rule-to-anchor renumbering, iteration aggregator rebinding) — a single one-off artifact-plus-human-judgment check cannot serve as a repeatable per-task gate the way a green test suite can, and elevating B to Primary would violate the Evaluation Contract's own singular+agent-runnable+reused-every-stage requirement (flow.md lines 224-228). (3) What this choice assumes: that an extended pytest suite (existing test_format_validators.py additive checks + rebound test_scar_schema_noise_rules.py death tests + new length-budget fixtures) is sufficient signal of 'done' for every task this feature decomposes into, and that a one-time golden-artifact check performed once during planning's acceptance step (not repeated per-task) is enough to satisfy kill_condition #3 without needing to be re-run as an ongoing evaluator. (4) What rots first if that assumption breaks: exactly the decoupling scenario the kickoff's own North Star sub_metric already names — 'validator 全綠但人類在 validate-and-ship 仍讀不懂 report（主觀抽查）' — i.e. tests stay green while the length-budget thresholds drift into either honesty-damaging (too tight) or noise-permitting (too loose) territory and nobody re-checks against real scar content after the one golden pass; the kickoff's own decoupling_detection clause is the named catch for this, so the rot path is not silent, it is instrumented. If B had instead been made Primary or co-Primary, the rot path would be different and worse: a single example passing would get treated as proof of general slot expressiveness across all future scar reports, and there would be no mechanical, repeatable gate for any of the other 5+ implementation tasks this feature spans."
- decision: proceed
- rationale: "This is the exact fork flow.md §6(2) anticipates: TDD/tests may be Primary only via an explicit recorded choice, and Option C is that explicit choice while also honoring the Evaluation Contract's harder constraint that the Primary be singular, agent-runnable, and reused unmodified by every downstream stage (planning/iteration/debugging/validate-and-ship) rather than re-invented per stage. Golden re-expression fails that reuse test on its own (single example, judgment-laden, not naturally repeated per task) but is exactly the artifact D2 in pre-thinking.md already committed to needing inside planning's acceptance criteria to cover kill_condition #3 — so demoting it to supporting evidence rather than dropping it is the only option that keeps both the Evaluation Contract's mechanical-reuse rule and the feature's own already-written kill condition intact."
- principles_used:
  - "flow.md §6(2) Evaluation Contract rules: Primary evaluator must be singular; TDD/death-path tests may be Primary only via explicit recorded choice; planning/iteration/debugging/validate-and-ship must reuse the same Primary, never invent a new standard per stage"
  - "D7 seam 'format-vs-judgment' (pre-thinking.md, evidenced by validate_format.py:2-8 docstring): mechanical/format checks belong in the validator+test layer, judgment ('honest lossless' semantic quality) belongs to reviewers — this is why golden re-expression's judgment component disqualifies it from being the sole canonical machine-checkable standard"
  - "D2's own text already commits to placing a compression-honesty contrast check inside planning's acceptance criteria — Option C operationalizes that commitment rather than re-deciding it"
  - "kill_condition #3 (problem-autopsy.md) requires an honesty-preservation check that cannot be satisfied by synthetic pytest fixtures alone"
- architecture_considerations:
  - "Evaluation Contract is a cross-stage single-standard channel (flow.md: 'Planning, iteration, debugging, and validate-and-ship must reuse this same Primary evaluator') — choosing a per-task-repeatable mechanical suite as Primary keeps that channel coherent across this feature's multi-task decomposition (schema, validator, dispatch-template, rule-renumbering, aggregator rebinding all under D1-D6)"
  - "keeping B as a planning-stage acceptance artifact (not a recurring gate) matches its one-time nature: it validates the *slot design's expressiveness* once against the worst historical case, not the *validator's behavior* on every future report — conflating the two would either make B unrepeatable-yet-Primary (broken) or make A blind to the honesty risk (also broken); splitting them along the format/judgment seam is the only architecture that serves both"
- evidence_checked:
  - "changes/2026-07-07_scar-report-subtraction/pre-thinking.md D2 (length-budget lives in validate_format.py, over-budget = finding, 'planning必须在acceptance裡放壓縮不失誠實的對照驗證'), D7 seam 'format-vs-judgment', A7 compression exercise ('單例證據，閾值最終校準交 planning')"
  - "changes/2026-07-07_scar-report-subtraction/problem-autopsy.md kill_conditions #3 ('長度預算無法在不傷誠實的閾值下運作')"
  - "changes/2026-07-07_scar-report-subtraction/1-kickoff.md North Star sub_metric decoupling_detection ('驗算法全綠但人類在 validate-and-ship 仍讀不懂 report')"
  - "skills/pre-thinking/flow.md lines 209-229 (Evaluation Contract exact structure and rules)"
  - "skills/implement/scripts/validate_format.py:2-8 docstring (format-vs-judgment split, read live)"
- uncertainty:
  level: medium
  notes: "This question was explicitly routed to the gatekeeper as a Step 4 'needs external call' item because Step 4 itself (D2's text) already leaned toward needing both A and B without formally choosing between the three framings — my resolution follows the Evaluation Contract's own written constraints (singular, reused-per-stage) plus the feature's own already-written kill condition and North Star text, so this is principle-derived rather than a pure preference guess; it is NOT marked 'unconfirmed guess' because no piece of it depends on information only the repo owner could supply (no naming convention, no business priority tradeoff was needed to resolve it). Residual uncertainty is at the planning layer: exact numeric length-budget thresholds and exactly how the golden re-expression contrast table gets task-ified inside planning's acceptance criteria are not decided here and remain planning's job per D2's own text."
- consequences:
  - "Main agent writes into pre-thinking.md verbatim, under the Group 1 question and under Step 6's Evaluation Contract: Primary evaluator = expanded pytest suite (`uv run pytest tests/`) including new length-budget behavior tests (over-budget fixture produces finding, clean fixture does not) and rebound compat death tests; Pass signal = all green; Fail signal = any red or any missing test for the new length-budget checks; Feedback loop = fix the validator/schema/rebind location the failing test names, per D3's compat-rule relocation; Out of scope validation = subjective 'is this actually readable' judgment, left to reviewer spot-check per the North Star's own decoupling_detection. Golden re-expression of task-4-scar.yaml is carried forward as a named supporting acceptance artifact that planning must task explicitly (not invented fresh at planning time, not treated as optional) to satisfy kill_condition #3."

## Decision 003 - pre-thinking.step6.commitment
- decision_id: decision-003
- timestamp: 2026-07-07T10:08:14Z
- stage: pre-thinking
- prompt_type: confirmation
- workflow_prompt: "Pre-thinking 完成 — commitment 是哪一個？ `Proceed`（進 planning）／`Accept gap`（帶著已知缺口進 planning，缺口須列名）／`Return to Research`（有 research 才能答的未解問題）。"
- gatekeeper_answer: "Proceed. I checked pre-thinking.md against its own planning-ready bar (SKILL.md/flow.md K3b + Section 6 closing check) plus the three specific scrutiny points named in the gate prompt, and none surfaces a defect requiring revise or a gap requiring accept_gap. (1) Core identity operability test: Step 2's identity ('schema is a write contract, validator is its only toothed enforcer, anything not machine-consumed and not action-changing does not belong') is demonstrated adjudicating two opposite concrete choices (review-history-in-narrative -> excluded; verbosity-fix-via-Rule-18-vs-validator-check -> validator) -- passes the operability test, not a slogan. (2) Every Step 4 dimension (D1-D7 + Group 1 evaluator question) lands in one of the three boxes with a written chain, and Step 4's own type-checklist closing check is present and answered ('無帶著缺口的維度；全部落入三盒'). (3) All three D7 real seams (scar-write-contract, write-read-boundary, format-vs-judgment) carry already-happened tier with git commit citations (af437a5, eb4ae07) plus dispatch-template.md/validate_format.py line citations -- no imagination-tier seam. (4) Evaluation Contract has exactly one Primary evaluator (expanded pytest suite) with explicit pass/fail signals, per decision-002; golden re-expression is correctly demoted to supporting evidence, not a second Primary. (5) All 5 residual list items are genuinely planning/calibration-scope (threshold calibration against historical distribution, dist/ regeneration, named-anchor batch rename, a named-and-accepted scope boundary already decided rather than an open design question, and aggregator test coverage) -- none require design authority pre-thinking lacks; this matches the closest sibling feature's own precedent (2026-07-02_workflow-subtraction-optimization Step C: 'Accepted gaps: none' with comparable planning-deferred items N1/I1). On the three specific scrutiny points: (a) A7's single compression example (task-4's longest/hardest known_shortcuts item, 60 lines -> 4-line slot rewrite, no honesty loss per its own diff-mapping) is sufficient evidence for pre-thinking's actual claim -- 'slot expressiveness is feasible in principle for the worst known case' -- not for the separate, correctly-deferred claim 'exact numeric thresholds are correct for all future reports' (residual #1 names that gap explicitly, and decision-002's supporting-evaluator obligation already binds planning to re-verify honesty-preservation with the golden artifact, so kill_condition #3 is not resting on the single example alone). (b) D4's cross-file rule-number citation claim I independently re-verified by grep across agents/implementer.md, skills/iteration/SKILL.md, skills/implement/scar-report.md, skills/implement/scripts/validate_format.py, and skills/implement/SKILL.md: actual count is ~14 citation lines across 5 files (pre-thinking's Lens B named 4 files and said '8+', an undercount that additionally omitted skills/implement/SKILL.md:184's Rule 11/14 citation) -- but '8+' was written as an open lower bound, not a closed inventory, and residual item #3 already commits planning to a full same-batch sweep before executing the rename, so the undercount does not invalidate the design decision (self-derived from a nameable root: existing numbered-reference mechanism has no verification mechanism per Lens B's git-accretion evidence, and this feature's own rule deletions force dangling refs under the old scheme regardless of choice) -- it only means planning's sweep must be more thorough than pre-thinking's approximate count suggested, which I am flagging as a sharpened note on the existing residual item, not a new gap. (c) I read tests/test_skills/test_scar_schema_noise_rules.py directly: test_death__backward_compat_rules_survive_and_extend_to_new_forms (lines 248-288) does pin Rules 7/8/14 compat phrases ('missing deferred flag = false', 'plain string format backward compatibility', 'rules 7 and 8 continue to apply unchanged', 'additive') against _rules_section(read(SCAR_SCHEMA)) -- i.e. scar-schema.yaml itself, confirming Lens A's finding that this specific death test currently lives on the schema side, in apparent tension with D3's plan to move compat rules to iteration SKILL.md. But this is not a silent, unaddressed contradiction: D3's own text names this exact test and commits to rebinding it in the same batch, and the precedent it cites is real and independently verified by me -- test_death__iteration_registry_missing_marks_unknown_not_silent_drop (lines 213-245) and test_unit__iteration_step1_documents_all_three_systemic_ref_resolution_paths (lines 604-621) already read ITERATION (skills/iteration/SKILL.md), not the schema, for closely-related systemic_ref/resolution content in this same test file -- so 'read iteration/SKILL.md as canonical home and rebind the assertion there' is an established pattern within this exact file, not a novel or risky move. This is a properly surfaced design consequence with a checkable execution path, not a hidden research gap or an overlooked contradiction."
- decision: proceed
- rationale: "Every item on pre-thinking's own planning-ready bar (identity operability, three-box closure, seam evidence tiers, single-Primary Evaluation Contract, planning-scope-only residual list) is independently verifiable and holds. The three scrutiny points the gate asked me to dig into (A7's single-example basis, D4's cross-file citation evidence, and the D3/Lens-A death-test tension) each check out on direct inspection of the live test file and cross-file grep: none reveals a design decision resting on invented evidence, an unresolved contradiction, or a threshold/inventory claim being overstated beyond what pre-thinking is entitled to claim at this stage. Sending this back for revise would require naming a concrete defect in the artifact; I found none -- only a minor undercount in an already-open-ended '8+' citation estimate, which the existing residual item #3 already structurally absorbs (full sweep is planning's job regardless of the exact count). Proceed matches this repo's own established convention (2026-07-02 sibling feature) that planning-deferred calibration/mechanical items do not by themselves demote a clean pre-thinking to accept_gap."
- principles_used:
  - "flow.md Section 6/Section 9 K3b: 'Decision: Proceed' + complete Evaluation Contract + L1 (core identity + real seams) = planning-ready; a Step 6 without these is incomplete, but pre-thinking.md here has all three"
  - "flow.md Section 4 three-box discipline and closing check ('every dimension that grew has landed in one of the three boxes') -- verified against D1-D7 and Group 1, all boxes filled with written chains"
  - "flow.md Section 4 evidence-tier marker for real seams (already-happened > planned-change > domain-essential > imagination; imagination is not evidence) -- all three D7 seams cite already-happened git/file evidence"
  - "project convention/precedent: 2026-07-02_workflow-subtraction-optimization Step C commitment ('Accepted gaps: none' despite N1/I1 planning-deferred items) -- this repo's own established norm for what counts as a genuine gap vs. a normal residual/handoff item"
  - "asymmetric friction (flow.md cross-cutting principle 2): challenging an assumption is free -- I re-verified rather than trusted pre-thinking's own citations for D4 and D3/Lens-A, and the re-verification held"
- architecture_considerations:
  - "D3's write-read-boundary seam (compat rules migrating from schema to iteration SKILL.md) has a real, load-bearing consequence on an existing death test (test_death__backward_compat_rules_survive_and_extend_to_new_forms); this is exactly the kind of coupling this gate should surface -- verified it is named and owned (D3's own text + planned rebind), not silently left for implementation to discover as a surprise red test"
  - "D4's rule-number-to-named-anchor migration touches more citation sites (~14 across 5 files) than pre-thinking's approximate count named (8+ across 4 files) -- this is a blast-radius correction that raises the stakes of residual item #3's 'same batch' requirement slightly, worth carrying forward as a sharpened note so planning's task decomposition does a fresh grep rather than trusting pre-thinking's list as exhaustive"
  - "Evaluation Contract's single-Primary discipline (decision-002) means both A7's slot-expressiveness claim and D4/D3's rebinding claims will get a second, independent mechanical check at planning/implementation time (pytest green + golden re-expression) -- this gate's Proceed is not the last checkpoint on any of these three scrutiny points, which lowers the cost of being wrong here"
- evidence_checked:
  - "changes/2026-07-07_scar-report-subtraction/pre-thinking.md full read (Steps 1-6, Evaluation Contract, Residual list)"
  - "changes/2026-07-07_scar-report-subtraction/auto-decisions.md decisions 001-002 (full read)"
  - "skills/pre-thinking/flow.md full read (six-step procedure, three-box discipline, seam evidence tiers, Step 6 commitment format, Section 9 K3b completion criteria)"
  - "references/auto-mode.md full read (Decision Log Contract, Required Fields, decision-value semantics)"
  - "AGENTS.md full read (project execution conventions, pytest/uv invocation)"
  - "tests/test_skills/test_scar_schema_noise_rules.py direct read (lines 1-300, 590-680): confirmed _rules_section() reads scar-schema.yaml only; confirmed test_death__backward_compat_rules_survive_and_extend_to_new_forms (248-288) pins Rules 7/8/14 compat phrases to the schema side; confirmed test_death__iteration_registry_missing_marks_unknown_not_silent_drop (213-245) and test_unit__iteration_step1_documents_all_three_systemic_ref_resolution_paths (604-621) already read skills/iteration/SKILL.md for closely-related resolution content -- the precedent D3 cites is real"
  - "grep -rn 'Rule' across agents/implementer.md, skills/iteration/SKILL.md, skills/implement/scar-report.md, skills/implement/scripts/validate_format.py, skills/implement/SKILL.md, skills/implement/dispatch-template.md: found ~14 citation lines across 5 files, one more file (skills/implement/SKILL.md:184) than pre-thinking's D4 paragraph named"
  - "changes/2026-07-02_workflow-subtraction-optimization/pre-thinking.md Step C (sibling-feature commitment precedent: Proceed + Accepted gaps: none despite comparable planning-deferred items)"
- uncertainty:
  level: medium
  notes: "Not a gap in pre-thinking's own bar, but a residual note I am carrying forward for planning's attention: D4's actual cross-file citation surface is larger (~14 lines / 5 files) than pre-thinking's '8+ / 4 files' approximation, so planning must run its own fresh grep before executing the named-anchor batch rename rather than trusting pre-thinking's list as exhaustive -- residual item #3 already structurally requires this ('同批更新'), so no new residual item is needed, only this sharpened note. Separately, A7's threshold numbers remain explicitly planning-calibrated (not this gate's job to validate) and D3's rebind of the schema-side death test remains an unexecuted commitment until planning/implementation actually does it -- both are visible, owned, and covered by the single Primary evaluator (decision-002), not silent."
- consequences:
  - "Main agent appends the Commitment block (Decision: Proceed, Accepted gaps: none) to the end of pre-thinking.md in flow.md Section 7 style, then proceeds to invoke samsara:planning for scar-report-subtraction. Planning must: (i) treat D1-D7 and the core identity/real-seams L1 handoff as given, not re-derive them; (ii) calibrate exact length-budget thresholds against historical scar-report compression distribution (residual #1); (iii) task the dist/codex+dist/gemini-cli schema regeneration (residual #2); (iv) run a fresh, exhaustive grep for rule-number citations (not limited to pre-thinking's '8+ / 4 files' estimate) before executing the named-anchor batch rename, explicitly including the rebind of test_death__backward_compat_rules_survive_and_extend_to_new_forms to its new canonical home (residual #3, sharpened); (v) record the fast-track/validate_format.py boundary as a named known limitation, not silently drop it (residual #4); (vi) task aggregator test coverage for the three-generation field-shape inference (residual #5); (vii) task the golden re-expression of task-4-scar.yaml as a required (not optional) supporting acceptance artifact per decision-002."

## Decision 004 - planning.plan-to-implementation-gate
- decision_id: decision-004
- timestamp: 2026-07-07T10:24:48Z
- stage: planning
- prompt_type: confirmation
- workflow_prompt: "Planning 完成。2-plan.md、acceptance.yaml、index.yaml 和 6 個 tasks 已就緒。確認後進入 Implementation？"
- gatekeeper_answer: "Revise. Planning clears most of its own bar (pre-thinking commitments consumed verbatim in overview.md's Core Identity/Real Seams/Key Decisions; death-first acceptance.yaml with all 5 death cases DC-A..E each owning a scenario and a task via 'Covers:' in the task files; task-5's golden fixture is explicitly NOT exempted from the budget per its own Death Test Requirement ('golden 本身被預算約束，不得特赦'); File Map Consistency Check present with matches/out-of-scope classification and no contradicts). But I independently re-derived the two arithmetic/self-containment claims the gate asked me to scrutinize and found two concrete, verifiable defects, not stylistic nitpicks: (1) task-1.md Step 3's literal token-preservation list for the test it says it rebinds (test_death__backward_compat_rules_survive_and_extend_to_new_forms, tests/test_skills/test_scar_schema_noise_rules.py) lists only 4 of the 6 substrings that test function actually asserts against _rules_section(). I read the live test file and confirmed it also asserts 'rules 7 and 8 continue to apply unchanged' and 'additive', neither of which appears in task-1.md's Step 3 bullet list of tokens to carry into the new iteration/SKILL.md section. If task-1 is executed literally as scoped, the rebound test fails at task-1's own Step 6 gate ('uv run pytest ... 綠') -- this is exactly the ordering-soundness failure mode the gate prompt named, not a hypothetical: I grepped the live scar-schema.yaml (line 132-136) and confirmed both missing substrings exist there today, verbatim, and are not yet duplicated into skills/iteration/SKILL.md. (2) 2-plan.md section 2's worst-case budget arithmetic is unsound as written: '最壞誠實尾端 14 條 × 6 行 + 標頭 10 + narrative 10 ≈ 90' computes to 14*6+10+10=104, not ~90 (off by 14 lines / ~15%, not rounding noise) -- I recomputed this by hand twice to rule out my own error. Since the report-level cap (≤90 lines) is the number this calculation is supposed to justify, and that cap is then propagated identically into both scar-schema.yaml (task-2) and validate_format.py module constants (task-3, explicitly '與 schema 預算表同值') -- the exact two-places-must-agree pattern this feature exists to eliminate -- shipping a load-bearing number whose own derivation doesn't support it risks baking a miscalibrated value into two synchronized copies before task-5's golden re-expression could empirically catch it. A secondary, related finding: task-4.md's Step 3 grep command ('(schema )?Rules? [0-9]', case-sensitive) would not match the lowercase 'rules 7 and 8 continue to apply unchanged' phrase once it lives in iteration/SKILL.md (capital-R 'Rule'/'Rules' required by the pattern; the phrase is lowercase mid-sentence) -- so even after defect (1) is fixed, DC-D's own dangling-citation death test has a documented blind spot for this specific known instance unless the grep is made case-insensitive or the phrase is rewritten to drop the numbers within task-1/task-4's scope."
- decision: revise
- rationale: "Both defects are concrete, evidence-verified, and map directly onto the exact scrutiny items the workflow prompt named (task ordering soundness at 4a, budget arithmetic at 4b) -- this is not generic caution. Both are cheap to fix now (edit two artifacts before any task dispatch) and expensive to fix later: defect (1) would surface as a red suite mid-task-1 requiring the implementer to improvise a fix not written in the task, breaking the audit standard that tasks must be self-contained; defect (2) would bake a self-contradicting number into scar-schema.yaml and validate_format.py in the same batch (task-2, task-3), both citing each other as the source of truth, so a later correction would itself have to touch two files in lockstep -- the exact drift pattern D3/D4 of this very feature were designed to prevent. Sending this back for revise while the fix is still a plan-only edit is the proportionate response; proceeding as-is would let a planning-stage arithmetic and citation-completeness error propagate into implementation artifacts."
- principles_used:
  - "Audit standard for this gate: 'is the ordering argument airtight' and 'is the arithmetic in 2-plan.md §2 sound' were explicit named scrutiny items in the workflow prompt, not open-ended review -- a gatekeeper that only checked the generic five-point bar without independently re-deriving these two specific claims would be performing ceremony, not judgment (user-prefers-honest-process-over-ceremony)"
  - "Project axiom operationalized in this feature's own core identity: 'schema is the write contract, validator is its only toothed enforcer' -- a budget number that cannot survive its own worst-case derivation undermines exactly the kill_condition (#3, budget must not damage honesty) this feature exists to satisfy"
  - "D3/D4 (this feature's own design decisions) explicitly diagnose 'two places drift' as the failure mode being fixed (compat rules split between schema/iteration; rule-number citations rotting silently) -- letting the SAME failure pattern (task-2's schema budget table and task-3's validator constants both citing an unsound-derivation number) into implementation would be the feature undermining its own thesis"
  - "Audit Standard (this skill's own instructions): tasks must be self-contained with exact literal contract sources, not partial lists an implementer must silently complete -- task-1's incomplete token list violates this"
- architecture_considerations:
  - "write-read-boundary seam: task-1's rebind is the hinge that lets task-2 delete schema text without breaking the suite; an incomplete token list at that hinge is a single point of failure for the entire 6-task ordering argument, not a cosmetic doc gap"
  - "scar-write-contract seam: the report-level ≤90 line budget is consumed identically by scar-schema.yaml (declaration) and validate_format.py (enforcement) per task-2/task-3's own cross-reference ('與 task-3 validator 同值' / '與 schema 預算表同值') -- an unsound derivation propagates into two synchronized artifacts in the same task batch, raising the cost of a later correction"
  - "DC-D's dangling-citation sweep (task-4) is a mechanical grep-based death test; a case-sensitivity gap in that mechanism against a known, named instance (the lowercase 'rules 7 and 8' phrase) is a coverage hole in the exact defense this feature is building, worth closing at plan time rather than discovering it only if/when someone notices the missed instance later"
- evidence_checked:
  - "tests/test_skills/test_scar_schema_noise_rules.py lines 1-300 (direct read): test_death__backward_compat_rules_survive_and_extend_to_new_forms asserts 6 substrings total, including 'rules 7 and 8 continue to apply unchanged' and 'additive', against _rules_section(read(SCAR_SCHEMA)).lower()"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-1.md Step 3 bullet list of tokens to preserve -- confirmed only 4 of the 6 required substrings are named"
  - "skills/implement/templates/scar-schema.yaml lines 110-150 (direct read): confirmed live text of Rule 14 block contains 'rules 7 and 8 continue to apply unchanged' (lowercase r) and 'ADDITIVE'; confirmed skills/iteration/SKILL.md does not yet contain either string (grep -n -i check, zero matches for 'additive' or 'rules 7 and 8')"
  - "changes/2026-07-07_scar-report-subtraction/2-plan.md section 2: hand-recomputed '14 條 × 6 行 + 標頭 10 + narrative 10' = 84+10+10 = 104, not the stated '≈90' -- recomputed twice, confirmed not a rounding artifact"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-2.md and task-3.md: confirmed both cite the report-level ≤90 value as shared/same-value ('與 task-3 validator 同值' / '與 schema 預算表同值'), confirming the two-copies propagation risk"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-4.md Step 3 literal grep command '(schema )?Rules? [0-9]' -- confirmed case-sensitive (capital R required), tested by inspection against the lowercase live phrase"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-5.md Death Test Requirements: golden fixture budget non-exemption confirmed present and explicit"
  - "changes/2026-07-07_scar-report-subtraction/acceptance.yaml and overview.md: DC-A..E each traced to a scenario and an owning task via task files' 'Acceptance Criteria: Covers:' lines -- all 5 present, none orphaned"
  - "overview.md Core Identity / Real Seams text diffed by eye against pre-thinking.md Step 6 L1 handoff text -- verbatim match confirmed"
- uncertainty:
  level: low
  notes: "The two primary defects (task-1 token list, 2-plan.md §2 arithmetic) are directly verified against live file contents and plain arithmetic, not inference -- low uncertainty on their existence. Medium-to-low uncertainty on severity: it is plausible the implementer would notice the gap organically during task-1's Step 2 ('跑 death tests -- 確認紅') and task-5's actual golden fixture may not reach the theoretical 14-item/6-line worst case in practice, so neither defect is guaranteed to cause an actual failure -- but 'the plan's own written derivation doesn't support its own chosen number' and 'a task's own literal instructions don't satisfy the test it names' are defects in the artifact regardless of whether a sufficiently careful implementer would route around them, and this gate's job is to judge the artifact, not to assume a downstream repair."
- consequences:
  - "Main agent must revise: (a) tasks/task-1.md Step 3's token-preservation bullet list to include 'rules 7 and 8 continue to apply unchanged' (or its post-renumbering rephrasing, coordinated with task-4's scope) and 'additive', so the rebound test in task-1's own Files/Death Test Requirements actually passes at task-1's Step 6; (b) 2-plan.md section 2's worst-case arithmetic -- either recompute honestly (84+10+10=104) and raise the report-level budget accordingly with a matching update to task-2's schema table and task-3's validator constants, or lower the item/narrative/header assumptions so the stated ≈90 is actually derived, not asserted; (c) task-4.md Step 3's grep pattern to be case-insensitive (or otherwise account for the lowercase 'rules 7 and 8' instance) so DC-D's death test has no documented blind spot for the specific token this gate traced. This gate must be re-evaluated against the revised 2-plan.md/tasks/task-1.md/tasks/task-4.md before Implementation may begin; the workflow does not proceed on the current artifacts."

## Decision 005 - planning.plan-to-implementation-gate (re-run after revise)
- decision_id: decision-005
- timestamp: 2026-07-07T10:30:01Z
- stage: planning
- prompt_type: confirmation
- workflow_prompt: "Planning 完成。2-plan.md、acceptance.yaml、index.yaml 和 6 個 tasks 已就緒。確認後進入 Implementation？ (re-run after revise, decision-004)"
- gatekeeper_answer: "Proceed. I independently re-verified all three named defects from decision-004 plus the one derived hardening, against live files, not against the summary claiming they were fixed. (1) task-1.md Step 3's token-preservation list: I read tests/test_skills/test_scar_schema_noise_rules.py:248-288 directly and confirmed test_death__backward_compat_rules_survive_and_extend_to_new_forms asserts exactly 6 substrings against _rules_section(read(SCAR_SCHEMA)).lower(): 'missing deferred flag = false', 'missing resolved_items = no self-iteration', 'plain string format backward compatibility', 'do not reject or silently skip plain string format items', 'rules 7 and 8 continue to apply unchanged', 'additive'. task-1.md Step 3 now lists all 6 verbatim (previously only 4). I also grepped the live scar-schema.yaml and confirmed both previously-missing tokens exist there today at lines 132-135 ('rules 7 and 8 continue to apply unchanged' at :132, 'ADDITIVE' at :135, matched case-insensitively as the test does via .lower()) -- so the list is not just complete on paper, it matches what the rebind will actually need to carry to survive task-1's own Step 6 gate. task-1.md also now carries an explicit transitional note: these two rule-numbered tokens are preserved verbatim by task-1 (test assertions unchanged) and only task-4 (which owns the de-numbering) rewrites both the iteration-side text and the test token together -- this correctly assigns ownership and prevents task-1 from prematurely rewriting a token task-4's grep needs to still find. (2) 2-plan.md section 2 arithmetic: I hand-recomputed every stated number. Heavy tail: 14*4(avg)+10(header)+10(narrative)=76, correctly stated as '≈76, within 90'. Absolute worst: 14*6(cap)+10+10=104, matches my own decision-004 recomputation exactly and is now stated as 104 (not falsely rounded to ~90). The plan explicitly labels the 104>90 case as deliberate compression pressure on the pathological tail, names three concrete escapes (route to the 去別處對照表, systemic_ref dedup, or task re-split) that are consistent with mechanisms already defined earlier in the same section, and names an explicit recalibration path via kill_condition #3 gated on task-5's golden re-expression -- this is now a self-consistent, non-circular derivation rather than an asserted number. The five-value budget table below matches task-3 Step 5's implementation constants and task-3's own new death test (schema-vs-validator-constant equality, mechanically parsed) closes exactly the two-places-drift risk this arithmetic feeds into. (3) task-4.md Step 3: grep is now `grep -rniE '(schema )?rules? [0-9]'` (case-insensitive, -i confirmed), the Death Test Requirements section states the pattern as `(?i)(schema )?rules? [0-9]` and explicitly names 'rules 7 and 8' as a variant it must catch, and Step 3's prose explicitly requires the sweep to catch the lowercase phrase task-1 relocated, coordinating the token/text rewrite with task-1's transitional note ('與 task-1 Step 3 的過渡註記交接'). (4) Derived hardening: task-3.md Death Test Requirements now includes a budget-value drift test that mechanically parses the schema's numeric budget table and asserts equality against validate_format.py's module-layer constants, with either side changing alone going red -- this is the correct mechanical (not judgment) form for the format-vs-judgment seam this feature's own identity establishes. No new contradiction found: the File Map Consistency Check, task depends_on ordering (1 -> 2 -> {3,4} -> 5 -> 6), and acceptance.yaml's DC-A..E scenario set are unchanged and still internally consistent with the revised arithmetic and token lists."
- decision: proceed
- rationale: "All three decision-004 defects are fixed at the level of evidence this gate requires -- not a claim re-read, but independently re-derived: the 6-token list checked against the live test assertions and the live schema file; the arithmetic recomputed by hand a third time (76 and 104 both reproduce); the grep pattern's case-insensitivity confirmed literally in both the Death Test Requirements prose and the Step 3 command. The one architecturally load-bearing risk decision-004 surfaced (a single unsound number propagating into two synchronized artifacts) is now closed two ways: honestly derived arithmetic AND a new mechanical death test (task-3) that would catch future drift between the two copies even if a later edit re-introduces disagreement. No new defect surfaced during re-verification; the fixes are targeted and did not disturb the parts of planning already found sound in decision-004 (Evaluation Contract, File Map, DC-A..E coverage, task self-containment elsewhere)."
- principles_used:
  - "Audit standard for this re-run gate: verify the revision actually resolves the named defect via live evidence, not via the revision's own self-description -- re-derived arithmetic independently rather than trusting 2-plan.md's stated recomputation"
  - "Project axiom operationalized: 'schema is the write contract, validator is its only toothed enforcer' -- the new schema-vs-validator-constant death test (task-3) is the correct mechanical enforcement of this axiom for the budget-table-agreement risk, closing the exact two-places-drift pattern this feature exists to eliminate"
  - "Audit Standard (this skill's own instructions): tasks must be self-contained with exact literal contract sources -- task-1's token list is now a complete, verified copy of the live test's actual assertions, not a partial list an implementer must silently complete"
  - "asymmetric friction: re-verifying token-for-token against the live test and schema file was cheap; trusting the revision's self-report would have been the cheaper-but-wrong path"
- architecture_considerations:
  - "write-read-boundary seam: task-1's rebind hinge now carries a complete and correctly-scoped token list, plus an explicit two-task ownership split (task-1 preserves verbatim, task-4 de-numbers) that prevents either task from silently redoing the other's work or leaving a token orphaned mid-migration"
  - "scar-write-contract seam: the ≤90 report-level budget is now honestly derived, and its propagation into scar-schema.yaml (task-2) and validate_format.py (task-3) is now guarded by a new mechanical equality test rather than resting on two independently-typed literals that could silently diverge on a future edit"
  - "DC-D's dangling-citation death test (task-4) now has no documented blind spot for the specific lowercase token this gate traced in decision-004; the case-insensitive pattern is stated identically in both the requirements prose and the literal Step 3 grep command, so there is no drift between the task's stated intent and its executable instruction"
- evidence_checked:
  - "tests/test_skills/test_scar_schema_noise_rules.py:248-288 (direct read): confirmed the exact 6 substrings asserted by test_death__backward_compat_rules_survive_and_extend_to_new_forms"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-1.md Step 3 (direct read): confirmed all 6 tokens now listed verbatim, plus explicit transitional-ownership note assigning de-numbering to task-4"
  - "skills/implement/templates/scar-schema.yaml (grep -n and grep -ni): confirmed 'rules 7 and 8 continue to apply unchanged' at line 132 and 'ADDITIVE' at line 135 exist live, matching the test's .lower() comparison"
  - "changes/2026-07-07_scar-report-subtraction/2-plan.md section 2 (direct read, hand-recomputed): 14*4+10+10=76 (heavy tail, matches stated ≈76); 14*6+10+10=104 (absolute worst, matches stated 104, matches my own decision-004 recomputation); confirmed 104>90 is now labeled deliberate with named escapes and a kill_condition #3 recalibration path"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-4.md Death Test Requirements and Step 3 (direct read): confirmed case-insensitive pattern `(?i)(schema )?rules? [0-9]` / `grep -rniE` and explicit 'rules 7 and 8' coverage claim, plus coordination language referencing task-1 Step 3's transitional note"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-3.md Death Test Requirements (direct read): confirmed new schema-vs-validator-constant equality death test present, mechanically parsed, either-side-alone-changing goes red"
  - "changes/2026-07-07_scar-report-subtraction/acceptance.yaml (direct read): DC-A..E scenarios unchanged and still each traceable to an owning task; no contradiction introduced by the three revisions"
  - "changes/2026-07-07_scar-report-subtraction/2-plan.md Task Summary depends_on chain (1 -> 2 -> {3,4} -> 5 -> 6) re-checked for consistency with the revised token/arithmetic content -- unchanged and sound"
- uncertainty:
  level: low
  notes: "All three defects and the one derived hardening are verified against live files (test source, schema source, task files), not against the revision's own narrative -- low uncertainty on whether the fixes are real. Residual medium-to-low uncertainty carried forward unchanged from decision-004 and decision-003: the exact numeric thresholds remain planning-calibrated and will get their empirical check at task-5 (golden re-expression); the median-case arithmetic (10*4+header≈50-60) is a looser range than the heavy-tail/worst-case figures and was not itself a named defect in decision-004, so it was not held to the same recompute-exactly bar, but it is not internally contradictory with the rest of section 2 either."
- consequences:
  - "Main agent proceeds to Implementation for scar-report-subtraction, dispatching task-1 first per the depends_on chain (task-1 has no dependencies), followed by task-2, then task-3/task-4 in parallel-eligible order (both depend on 1,2), then task-5, then task-6. Each task's own Death Test Requirements and Step 6 pytest-green gate remain the per-task acceptance mechanism; the golden re-expression (task-5) remains the empirical check on kill_condition #3's budget-vs-honesty tradeoff."

## Decision 006 - implementation.execution-strategy-selection
- decision_id: decision-006
- timestamp: 2026-07-07T10:33:49Z
- stage: implementation
- prompt_type: question
- workflow_prompt: "Plan 中有 6 個 tasks。依賴分析：task-1 無依賴；task-2 依賴 task-1；task-3 依賴 task-2；task-4 依賴 task-1+task-2；task-5 依賴 task-2+task-3；task-6 依賴 task-1..5（注意：即使 task-3 與 task-4 依賴圖上可並行，兩者都修改 tests/test_skills/test_scar_schema_noise_rules.py 與 skills/implement/scripts/validate_format.py — 檔案衝突；且 implement SKILL.md Red Flags 明文禁止多 implementer 並行）。執行模式：(A) Subagent parallel／(B) Subagent sequential／(C) Inline sequential。選哪個？"
- gatekeeper_answer: "(B) Subagent sequential — one fresh samsara:implementer subagent per task, dispatched strictly in depends_on order: task-1, task-2, task-3, task-4, task-5, task-6. I independently re-verified the stated file conflict rather than trusting the prompt's claim: tasks/task-3.md Files section modifies skills/implement/scripts/validate_format.py (Step 5, real length-budget/legacy-form/slot-required behavior) and tests/test_skills/test_format_validators.py; tasks/task-4.md Files section modifies the SAME skills/implement/scripts/validate_format.py (Step 4, message-wording-only rename to named anchors) plus tests/test_skills/test_scar_schema_noise_rules.py (new dangling-citation death test, Step 3/5 explicitly coordinating with task-1's compat-token rebind). This confirms the prompt's premise: task-3 and task-4 are graph-parallel (both depend only on task-1+task-2) but share a real, load-bearing file (validate_format.py) plus a chained dependency through task-1's Step 3 token list into task-4's Step 3/5 de-numbering handoff — so option (A) is doubly disqualified: (i) skills/implement/SKILL.md Red Flags line 241 states 'Dispatch multiple implementer subagents in parallel (file conflicts)' as a Never, with no task-specific exception clause, and (ii) even absent that rule, the concrete conflict is real and verified, not hypothetical. This collapses the choice to B vs C only, and since both are sequential with an identical task order, the only load-bearing difference is context origin (fresh subagent vs. main-agent-inline), not scheduling. I choose B over C for four reasons, in order of weight: (1) Design-target match — dispatch-template.md's entire Global Thinking Channel (L1 Global Position / L2 Context Projection / L3 task body / L4 deep reference, 'awareness is small in volume, so pushing it does not violate subagent context hygiene') and the Subagent Review section exist specifically to serve mode A/B dispatch; using mode C would leave this already-built, already-verified machinery unused for zero scheduling benefit (order is identical to B). (2) Context hygiene for an already-heavy main agent — this main agent's own context already carries five dense gatekeeper decisions spanning research→pre-thinking→planning (auto-decisions.md decisions 001-005, each with multi-paragraph independently-re-derived evidence chains); loading six more tasks' worth of file edits, pytest runs, and self-iteration into that same window (mode C) risks the exact cross-task context bleed (later task reasoning contaminated by earlier task's verbose planning-stage style, or by an earlier task's now-stale assumptions) that this workflow's L1-L4 narrow-projection design exists to prevent by construction; mode B gives each task a provably clean slate. (3) Role separation — mode C makes the main agent simultaneously the code author and the curator of its own diff/context for the code-reviewer and code-quality-reviewer dispatches, a tighter conflation of author/reviewer-dispatcher roles than mode B's fresh-implementer-subagent + main-agent-as-independent-dispatcher split; the Yin-Side Constraints' 'review before index update' discipline reads more cleanly against an externally-authored diff. (4) Auditable enforcement — mode B's samsara:implementer subagents auto-load agents/implementer.md (STEP 0, death-test ordering, scar-report format) per SKILL.md line 116 ('You do NOT need to inject these into the prompt'); mode C requires the main agent to self-enforce the same constraints (structural honesty, read-before-write, no-silent-dependency, per SKILL.md lines 218-229) without a loaded definition to check against later — a strictly harder-to-audit posture for a 6-task, multi-file-touching feature already found to need two rounds of planning-defect correction (decision-004). I acknowledge the token-cost tradeoff (B pays a known, bounded per-task paste cost that dispatch-template.md Rule 2 explicitly designs for; C would save that cost) but the project's own stated design value ('structural awareness must be broad, raw content must stay narrow') prioritizes context hygiene over token minimization, and task self-containment for this feature's 6 tasks was already independently verified twice (decision-004 found and decision-005 confirmed-fixed two concrete defects in task-1/task-4/2-plan.md precisely because tasks must stand alone for fresh-context dispatch) — that verification investment is wasted if mode C is chosen instead."
- decision: proceed
- rationale: "The workflow's own Red Flags rule makes option A a hard stop regardless of the depends_on graph, and I independently confirmed the concrete file-conflict evidence behind that rule rather than accepting the prompt's claim at face value. With A eliminated, B and C produce an identical task execution order (1→2→3→4→5→6, no genuine parallelism available anywhere in this plan), so the decision reduces to a context-architecture question the workflow already answers structurally: dispatch-template.md's Global Thinking Channel, Subagent Review section, and agents/implementer.md auto-loading are all built for and only used by modes A/B — choosing C would abandon that machinery for zero scheduling gain while adding a self-enforcement burden and context-dilution risk to a main agent that already carries a dense five-decision planning history. This is not a generic 'more caution is safer' call; it follows directly from re-reading the two task files' Files sections and the SKILL.md Red Flags/Yin-Side-Constraints text."
- principles_used:
  - "skills/implement/SKILL.md Red Flags (line 241): 'Dispatch multiple implementer subagents in parallel (file conflicts)' is an unconditional Never — no per-task exception for graph-parallel-eligible tasks that share a file"
  - "dispatch-template.md Global Thinking Channel design rule: 'structural awareness must be broad, raw content must stay narrow' — this is the project's own stated preference for context hygiene over token minimization, directly relevant to the B-vs-C tradeoff"
  - "skills/implement/SKILL.md line 116: samsara:implementer subagents auto-load agents/implementer.md constraints; mode C (lines 218-229) explicitly places the self-enforcement burden on the main agent instead — the SKILL.md itself frames this as the cost of choosing C, not a neutral option"
  - "project convention already active in this feature: task self-containment (complete literal token lists, explicit Files/Death Test Requirements) was twice independently verified (decision-004 defect-finding, decision-005 fix-verification) specifically because fresh-context subagent dispatch (mode B) depends on it — this investment argues for actually using mode B"
- architecture_considerations:
  - "write-read-boundary / scar-write-contract seams: task-3 and task-4 both touch skills/implement/scripts/validate_format.py (task-3 adds real behavior in Step 5, task-4 renames message strings in Step 4) and task-4 additionally touches tests/test_skills/test_scar_schema_noise_rules.py which chains back to task-1's compat-token rebind (task-4 Step 3/5 explicitly says '與 task-1 Step 3 的過渡註記交接') — this is a real cross-task coupling on both a shared file and a shared token-contract, not just a shared-directory coincidence, reinforcing that strict sequential dispatch (task-1 before task-4, task-3 before or after task-4 but never concurrent with it) is load-bearing, not precautionary"
  - "role separation: mode B keeps 'who wrote the code' (fresh implementer subagent, one per task) and 'who curates/dispatches the review' (main agent) as distinct actors across all 6 tasks; mode C merges them, which is a bigger deviation from the review architecture's implicit independence assumption than B's design"
  - "main-agent context budget: this feature's auto-decisions.md already holds 5 dense entries (~9000+ words of gatekeeper reasoning) before implementation even starts; adding 6 tasks' inline implementation work to the same context (mode C) compounds an already-heavy context load in a way mode B's per-task fresh-subagent isolation avoids by construction"
- evidence_checked:
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-3.md Files section (direct read): Modify skills/implement/scripts/validate_format.py, Test tests/test_skills/test_format_validators.py"
  - "changes/2026-07-07_scar-report-subtraction/tasks/task-4.md Files section (direct read): Modify skills/implement/scripts/validate_format.py (message wording), Test tests/test_skills/test_scar_schema_noise_rules.py (new dangling-citation death test); Step 3/5 explicitly reference coordinating with task-1's Step 3 compat-token handoff"
  - "skills/implement/SKILL.md line 241 (Red Flags, direct grep+read): 'Dispatch multiple implementer subagents in parallel (file conflicts)' listed under Never with no exception clause"
  - "skills/implement/SKILL.md lines 90-130 (Execution Mode Selection, Subagent Context) and lines 208-229 (Yin-Side Constraints, Inline mode C notes) direct read"
  - "skills/implement/dispatch-template.md full read: Global Thinking Channel table and design rationale, Subagent Context rules, Review Dispatch section"
  - "changes/2026-07-07_scar-report-subtraction/2-plan.md Task Summary table (depends_on: 1; 1; 2; 1,2; 2,3; 1-5) cross-checked against the workflow_prompt's dependency analysis — matches"
  - "changes/2026-07-07_scar-report-subtraction/auto-decisions.md decisions 004-005 (prior gatekeeper verification that task-1/task-4/2-plan.md self-containment was defective, then fixed and re-verified) — cited as the investment that presumes mode B's fresh-context dispatch"
- uncertainty:
  level: low
  notes: "The file-conflict premise and the Red Flags prohibition are both directly verified against live files, not inferred — low uncertainty that option A is disqualified. The B-vs-C choice itself is a legitimate judgment call (SKILL.md offers both as valid sequential modes and does not mandate one over the other), so I am recording it as a reasoned architecture preference rather than a bright-line rule: nothing in the skill docs makes C non-compliant, and if a later gate or the implementer finds mode B's per-task dispatch overhead is not paying for itself (e.g. tasks turn out simpler in practice than the plan's file-conflict analysis suggested), that would be a legitimate reason to revisit this choice, not evidence this decision was wrong when made."
- consequences:
  - "Main agent dispatches samsara:implementer subagents one at a time, strictly in the order task-1, task-2, task-3, task-4, task-5, task-6 (no concurrent implementer dispatches at any point, including the task-3/task-4 graph-parallel-eligible pair). Each dispatch follows dispatch-template.md verbatim (full task-N.md paste, curated overview.md sections, scar-schema.yaml paste, L1/L2 copied from index.yaml/overview.md). After each task's implementer reports back, main agent dispatches both code-reviewer and code-quality-reviewer in parallel per dispatch-template.md, resolves any Critical/UNKNOWN before updating index.yaml, then proceeds to the next task in the fixed order. Commit only after all 6 tasks and their reviews complete."

## Decision 007 - implementation.completion-transition-gate
- decision_id: decision-007
- timestamp: 2026-07-08T12:43:03Z
- stage: implementation
- prompt_type: question
- workflow_prompt: "All tasks complete. Iteration-entry criteria MET (signal_lost=12 >= threshold 5, AND a cross-task pattern found: systemic_ref doc-vs-runtime-obedience in task-1 and task-5 scar reports). Per implement SKILL.md Transition: recommend entering samsara:iteration or proceed to samsara:validate-and-ship?"
- gatekeeper_answer: "samsara:validate-and-ship (not samsara:iteration). I independently recomputed signal_lost from the six scar reports item-by-item against iteration SKILL.md Step 1's formula (known_shortcuts + silent_failure_conditions + assumptions_made where verified==false, excluding status:resolved/resolved_items-covered items, including deferred_to_feature_iteration:true items): task-1=1 (systemic_ref doc-vs-runtime-obedience, silent_failure_conditions), task-2=2 (systemic_ref doc-instruction-no-code-enforcement + anti-dup literal-token blindness, both known_shortcuts), task-3=2 (legacy-form-no-grandfather deferred_to_feature_iteration:true known_shortcut + doc-separator-uncovered silent_failure_condition), task-4=2 (skill-implement.yaml:42 collapsed-listing known_shortcut + rename-not-caught silent_failure_condition), task-5=3 (systemic_ref doc-vs-runtime-obedience known_shortcut + golden-fixture-misclassify silent_failure_condition + per-row-semantic-classification unverified assumption), task-6=2 (dist-gitignored-local-only known_shortcut + dist-md-non-byte-diffable silent_failure_condition) = 12, matching the reported figure exactly and confirming the threshold-met branch (not default-skip, not unknown). I also independently verified the cross-task pattern claim: systemic_ref doc-vs-runtime-obedience appears literally in task-1 (silent_failure_conditions) and task-5 (known_shortcuts), and confirmed against .samsara/systemic-scars.yaml that this id is a PRE-EXISTING registry entry (first_recorded: 2026-07-02, applies_when a task adds/modifies agent-facing instruction markdown) describing an inherent epistemic limit ('doc-presence tests... cannot prove an agent actually OBEYS the rule at runtime') -- this feature referenced an already-tracked systemic category, it did not discover new cross-task rot. Of the 12 remaining items, I read each one's own disposition text: 9 carry an explicit accepted_because/inherent-limit rationale already recorded during Level 1's both-reviewer-passed review (task-1's doc-vs-runtime residual, task-2's anti-dup regex limit, task-3's doc-separator gap, task-4's both items, task-5's doc-vs-runtime residual and its disclosed-by-design unverified per-row classification (decision-002 already named this as why golden re-expression is a supporting artifact, not Primary), task-6's both items); 1 (task-3's legacy-form-no-grandfather item) is formally deferred_to_feature_iteration:true but its underlying rot (task-1 scar report being in pre-migration Gen-2 form) already appears substantively resolved -- task-1-scar.yaml's own header comment records a 'Gen-2->Gen-3 一次性機械遷移 by main agent' and the file is now in Gen-3 what/bites_when slot form -- though task-3's report itself was never updated to mark this resolved, which I flag as a gap below rather than silently treating as closed. That leaves exactly 1 item (task-2's doc-instruction-no-code-enforcement / 'go-elsewhere table has no validator teeth') whose disposition is genuinely ambiguous: task-2's own note says 'validator teeth is task-3' but task-2's report was never updated to confirm task-3 actually closed it, and task-3's report does not explicitly claim credit for it. Given the cross-task pattern is a pre-existing, already-registered inherent limit (not new, not code-fixable -- iteration's Fix mechanism cannot resolve an epistemic doc-vs-runtime gap by writing more code, since the registry entry itself defines the gap as unobservable from the doc layer), and 9 of 12 remaining items are already-substantiated task-level accept decisions from a both-reviewer-passed process, entering iteration would mostly re-litigate settled, rationale-bearing residuals through a heavier per-item Fix/Accept/Defer + implementer/reviewer dispatch machinery -- disproportionate process weight for the actual remaining risk. validate-and-ship's Failure Budget Review (Step 1) is the workflow's designed safety net for precisely this skip: its systemic_ref resolution is 'mandatory when Level 2 iteration was skipped,' and it independently re-resolves every systemic_ref, re-runs the terminal format audit (validate_format.py), and Step 6 Code Review provides an additional yin-side pass -- so choosing validate-and-ship does not silently drop review coverage of these 12 items."
- decision: accept_gap
- rationale: "The workflow_prompt is answered with enough evidence to select validate-and-ship over iteration (independently reverified signal_lost=12 and the cross-task pattern, and confirmed the pattern is a pre-existing registered systemic limit rather than new actionable cross-task rot), but two residual uncertainties in my own review (task-2's go-elsewhere-no-teeth disposition, and task-3's deferred item's likely-but-unconfirmed resolution) must not be silently dropped just because they were not individually re-triaged here. accept_gap is the correct decision value rather than a bare proceed: it names these specific open items so validate-and-ship's Failure Budget Review -- which already mandatorily re-resolves scar-report items when iteration is skipped -- treats them as live findings rather than assuming this gate's analysis closed them."
- principles_used:
  - "skills/implement/SKILL.md Transition: iteration-entry criteria met triggers the gate (human ask / auto gatekeeper) to CHOOSE between samsara:iteration and samsara:validate-and-ship -- being 'met' does not mandate iteration; the gate exercises judgment on which path fits the actual remaining risk"
  - "skills/iteration/SKILL.md Step 1: signal_lost formula and parse-failure semantics are canonical there; iteration's Triage (Step 2) exists to Fix actionable rot and requires Accept classifications to carry a re-review signal + owner -- a heavier bar than task-level accepted_because, but not needed when the residual is an already-registered inherent limit rather than fixable code"
  - "skills/validate-and-ship/SKILL.md Step 1 Failure Budget Review: systemic_ref resolution is explicitly 'mandatory when Level 2 iteration was skipped' -- the workflow itself designs this as the catch-all for exactly this branch, not a weaker fallback"
  - ".samsara/systemic-scars.yaml doc-vs-runtime-obedience and doc-instruction-no-code-enforcement entries (both first_recorded 2026-07-02, pre-existing this feature) -- project convention of tracking inherent doc-contract-testing limits as a registry category rather than per-feature code fixes"
  - "user-prefers-honest-process-over-ceremony (memory): match process weight to risk -- entering iteration to re-process 9 already-rationalized, both-reviewer-passed residuals plus one pre-registered epistemic limit would be ceremony disproportionate to the remaining risk"
- architecture_considerations:
  - "Level 2 iteration's Fix mechanism operates on code-level actionable rot (implementer dispatch, death test, per-fix commit); the dominant cross-task pattern here is an epistemic category (doc-presence tests cannot observe runtime obedience) that the project already handles architecturally via a separate mechanism (.samsara/systemic-scars.yaml registry + design-level acceptance in pre-thinking.md/decision-002), not via iteration's per-fix loop -- routing it through iteration would apply the wrong tool to the wrong seam"
  - "validate-and-ship's Failure Budget Review and Terminal Format Audit are structurally positioned as the skip-path's safety net (mandatory systemic_ref resolution + validate_format.py re-run + Code Review), so choosing this path does not remove a checkpoint, it routes remaining uncertainty to the checkpoint designed to receive it"
  - "task-level scar reports (task-2, task-3) were not retroactively updated when later tasks (task-3, task-6) plausibly closed their residuals -- this is a real cross-task reporting-currency gap (reports are append-only per-task, not re-opened), independent of whether iteration or validate-and-ship is chosen next; naming it here prevents it from being read as 'resolved' by omission"
- evidence_checked:
  - "changes/2026-07-07_scar-report-subtraction/scar-reports/task-{1..6}-scar.yaml (direct read, all six, full contents) -- item-by-item signal_lost recount = 12, matches reported figure"
  - ".samsara/systemic-scars.yaml lines 30-51 (direct read): doc-vs-runtime-obedience and doc-instruction-no-code-enforcement both first_recorded 2026-07-02, both pre-existing this feature's 2026-07-07 kickoff"
  - "task-1-scar.yaml header comment ('Gen-2->Gen-3 一次性機械遷移 by main agent') and current Gen-3 what/bites_when shape -- cross-checked against task-3-scar.yaml's deferred_to_feature_iteration item which names task-1's then-Gen-2 format as the trigger"
  - "skills/iteration/SKILL.md Step 1 (signal_lost formula, parse-failure semantics, systemic_ref resolution three-branch procedure) and Step 2 (Triage accept-classification re-review-signal+owner requirement) -- direct read"
  - "skills/validate-and-ship/SKILL.md Step 1 Failure Budget Review ('systemic_ref resolution (mandatory when Level 2 iteration was skipped)') and Terminal Format Audit -- direct read"
  - "changes/2026-07-07_scar-report-subtraction/auto-decisions.md decision-002 (Primary evaluator = pytest suite; golden re-expression demoted to supporting artifact) -- cited as the pre-existing design-level acceptance of task-5's judgment-based classification gap"
- uncertainty:
  level: medium
  notes: "Medium, not low, for two named reasons: (1) task-2's go-elsewhere-no-teeth item's actual closure status is genuinely ambiguous from the reports alone (task-2 gestures at task-3 providing 'teeth'; task-3's report does not confirm this); (2) task-3's deferred_to_feature_iteration item appears substantively resolved by task-1's mechanical Gen-2->Gen-3 migration but task-3's own report was never updated to reflect this, so I am inferring resolution from a sibling file rather than an explicit in-report marker. Neither uncertainty is security/privacy-related (both are format/architecture-residual questions), so the high-uncertainty security reject rule does not apply. Both items are named explicitly as the accept_gap so validate-and-ship's Failure Budget Review treats them as open rather than closed-by-inference."
- consequences:
  - "Main agent does not dispatch samsara:iteration. Main agent proceeds to invoke samsara:validate-and-ship, whose Step 0 security/privacy STOP gate runs regardless of this choice (per the workflow_prompt's own note) and whose Step 1 Failure Budget Review must explicitly treat as live findings: (a) task-2's go-elsewhere-no-teeth disposition (confirm whether task-3 actually closed it or it remains open), (b) task-3's deferred_to_feature_iteration legacy-form item (confirm task-1's mechanical migration actually closed it and record that closure in task-3's report or ship-manifest.yaml rather than leaving it silently inferred), and (c) the two systemic_ref items (doc-vs-runtime-obedience, doc-instruction-no-code-enforcement) per its own mandatory systemic_ref resolution procedure. Both accept_gap items must also appear in ship-manifest.yaml's accepted_risks/known wounds if validate-and-ship's own review does not independently resolve them to closed."
