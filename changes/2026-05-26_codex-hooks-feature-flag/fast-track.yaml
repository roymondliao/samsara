type: fast_track
description: "Update Codex feature flag output from deprecated codex_hooks to hooks and bump release version to 0.9.2."
death_clause: "If samsara-cli writes [features].codex_hooks into Codex config.toml or release metadata drifts from 0.9.2, this change is failed."
acceptance:
  - "Codex conversion emits [features].hooks = true in .codex/config.toml."
  - "Project and global installs remove deprecated codex_hooks when writing hooks."
  - "Installer instructions show hooks = true for required Codex feature flags."
  - "Marketplace metadata, plugin metadata, pyproject.toml, and uv.lock declare version 0.9.2."
scar_tag: none
scar_items: []
files_changed:
  - "samsara_cli/config/platform/codex.yaml"
  - "samsara_cli/installer/install.py"
  - "tests/test_config/test_schema.py"
  - "tests/test_converter/test_engine.py"
  - "tests/test_installer/test_install.py"
  - "tests/test_installer/test_install_death.py"
  - ".claude-plugin/marketplace.json"
  - ".claude-plugin/plugin.json"
  - "pyproject.toml"
  - "uv.lock"
quality_checklist:
  - criterion: "C5 Reuse"
    checked: true
    note: "Reused the existing feature_flags flow for generated config and install-time TOML updates."
  - criterion: "C6 Clear Structure"
    checked: true
    note: "Kept platform selection in codex.yaml and TOML migration inside installer config merge/update helpers."
  - criterion: "C7 Elegant Logic"
    checked: true
    note: "Added a single deprecated-to-current feature flag map instead of branching throughout install paths."
  - criterion: "C8 No Redundancy"
    checked: true
    note: "The desired flag remains sourced from platform config; migration only removes obsolete aliases."
